# s3x 上下文

本文件定义 `s3x` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 角色与边界

**适配器**：把 S3 REST 协议（签名、寻址、对象读写、列举）收敛成稳定 Rust API 的库；
它不承载领域模型、不做业务分片或调度编排，也不实现对象存储服务端。
_Avoid_: S3 SDK（`aws-sdk-s3` 一类 SDK 含 smithy 代码生成、凭据供应链与更宽的协议面，
超出本仓库边界）

**配置**：构造期一次性校验、之后只读的声明式参数集合；校验失败即拒绝构造，
不提供「先构造、后补验」的路径。
_Avoid_: 运行时设置（本仓库没有可变更的运行时配置面）

**客户端**：承载签名、请求发送、背压与重试的句柄（`S3Client`），可 `Clone` 且克隆共享内部状态；
`connect` 只做校验与构造，不发网络请求。
_Avoid_: 连接（本仓库无长连接概念，每次请求独立完成）

**寻址风格**：把 bucket 放在主机名（virtual-hosted）还是路径（path-style）的决定；
它影响 URL 与签名，不改变对象语义。
_Avoid_: 端点（端点是 host 级别配置，寻址风格是 bucket 与 key 的拼接方式）

## 签名与凭证

**SigV4 签名**：`AWS4-HMAC-SHA256` 的完整计算过程，遵守 S3 的「规范化 URI 不做二次编码」
等规则；它是本仓库手写实现、并以 AWS 官方 `aws-sig-v4-test-suite` 向量为准的部分。
_Avoid_: 鉴权（签名是构造证明的过程，鉴权是服务端给出通过或拒绝的结果）

**规范化请求**：签名前把 method、URI、查询串与头折叠成唯一字符串的中间形态；
同一个逻辑请求必须只有一种规范化结果。
_Avoid_: 请求拼接（规范化有排序、编码与折叠规则，不是简单的字符串拼接）

**签名密钥链**：由 secret、日期、区域、服务与终止符逐层派生出的临时密钥；
它不回显任何原始凭据。
_Avoid_: 密码（派生密钥是不可逆中间量，与长期凭据不是同一层级的东西）

**凭据**：`access_key_id` 与 `access_key_secret`（以及可选的 `session_token`），
只能经环境变量或 builder 注入，`from_toml` 直接拒绝后两者。
_Avoid_: 配置项（凭据不是普通配置，它永不进入错误消息、`Debug` 输出或 URL）

**未签名载荷**：以 `UNSIGNED-PAYLOAD` 代替请求体哈希的签名模式；
在明文 HTTP 上必须显式开启，否则不启用。
_Avoid_: 跳过签名（请求仍然完整签名，只是载荷哈希被固定占位符替代）

## 对象与数据形态

**对象键**：经 `ObjectKey` 校验的键：非空、无前导 `/`、无 `..`、无控制字符、≤ 1024 字节；
它是数据面方法接受的唯一键形态。
_Avoid_: 路径（对象键不是文件系统路径，`..` 与绝对路径在此被显式拒绝）

**字节流**：下载返回的流式数据通道（`ByteStream`）；它持有并发额度直到被消费完或丢弃。
_Avoid_: 响应体（流强调消费生命周期与背压归属，不只是可读的字节）

**列举分页**：`list_objects_v2` 一次返回的单页结果（单页键数 ≤ `MAX_LIST_KEYS` = 1000）；
它不是全量清单。
_Avoid_: 目录列表（对象存储无目录层级，分页由服务端游标推进）

**预签名 URL**：把签名证据放进 query-string、供第三方在有效期内直接访问的临时链接；
有效期收敛在 1 秒 ~ 7 天，且正确性依赖本机时钟。
_Avoid_: 公开链接（预签名有明确失效时刻且绑定方法与键，不等同于公开暴露）

## 资源与可靠性

**并发背压**：以 `max_in_flight` 信号量限制在途请求数的机制；额度覆盖整个请求生命周期，
慢速大对象下载同样受上限约束。
_Avoid_: 连接池（本仓库限制的是在途请求，不是复用的连接对象）

**重试分类**：`is_s3_retryable` 对失败是否可安全重放的判定；网络 / 超时 / 5xx / 429 属瞬时，
4xx 鉴权与参数错误属永久且立即返回。
_Avoid_: 错误严重度（分类依据是可否重放，不是错误的严重程度）

**上界钳制**：对并发、超时、重试次数与预签名有效期在构建期校验并 clamp 到 `HARD_MAX_*`；
超限不是报错而是收敛。
_Avoid_: 参数校验（校验会拒绝非法输入，钳制是把合法但过大的取值压到上限）
